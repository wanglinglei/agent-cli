---
description: agents-cli Agent 扩展开发规范
globs:
  - "src/**/*.ts"
  - "README.md"
alwaysApply: true
---

# Agent 开发规范

本文档定义 `agents-cli` 后续扩展 Agent 时必须遵守的结构、命名、提示词、状态、日志、产物和验证标准。

## 1. 架构原则

- CLI 只有一个自然语言入口：`src/cli.ts`。
- 所有任务先进入 `routerAgent`，由 LangGraph 条件分支选择后续 Agent 流程。
- 新增能力优先注册 `src/graph/agentRegistry.ts` 中的 flow definition，不要新增独立 CLI 入口。
- `routerAgent` 的 route 候选和提示词说明必须由 flow registry 驱动，不要在 router 中手写业务 route 列表。
- 业务 flow 默认采用 ReAct 工具调用架构：Agent 节点准备上下文、调用本 flow 的 prompt builder、通过 `src/graph/reactToolRunner.ts` 执行 LangChain v1 `createAgent`，并通过本 flow 的 `PluginDataStore` 子类更新 `pluginData`。
- 不使用已弃用的 `@langchain/langgraph/prebuilt.createReactAgent`；标准 Agent harness 使用 `langchain` 包导出的 `createAgent`。
- 提示词、工具、状态类型、产物写入必须分层维护，不能混在 Agent 业务逻辑里。

## 2. 文件规范

所有 `src/**/*.ts` 文件必须带文件头注释：

```ts
/*
 * @Author: wanglinglei
 * @Date: 2026-05-27 19:16:50
 * @Description: 当前文件主要功能说明。
 * @FilePath: /agents-cli/src/path/to/file.ts
 * @LastEditTime: 2026-05-27 19:16:50
 */
```

要求：

- `@Description` 必须用中文说明当前文件职责。
- `src/cli.ts` 保留 shebang 第一行，文件头注释放在 shebang 后面。
- 新文件默认使用 ASCII；中文只用于注释、提示词和面向用户输出。
- 业务函数必须添加中文 JSDoc，说明输入、输出、状态变更和失败策略。

## 3. 目录职责

- `src/agents/<flow>/`：单个业务 flow 的 Agent 节点、提示词、私有状态、flow 注册定义和强业务依赖工具。
- `src/agents/<flow>/tools/`：仅当前业务 flow 使用的 ReAct 工具集合和业务专属工具实现。
- `src/agents/router/agents.ts`、`src/agents/unknown/agents.ts`：核心路由和兜底节点。
- `src/prompts/`：router、JSON 修复等公共 LLM 提示词模板和 prompt builder。
- `src/tools/`：跨业务 flow 可复用的公共基础工具，例如统一搜索封装、本地命令执行和风险检查。
- `src/graph/`：LangGraph 状态定义、flow registry、公共 `PluginDataStore` 基类、节点注册、边和条件分支，以及 ReAct 工具调用 runner。
- `src/graph/agentRegistry.ts`：内部 Agent flow 注册表，集中维护 route、路由说明、节点、边和条件分支。
- `src/graph/pluginData.ts`：业务流程私有状态存储基类；具体 flow 必须在自己的目录中继承。
- `src/graph/flowTypes.ts`：flow definition、节点和边的共享类型。
- `src/graph/reactToolRunner.ts`：封装 LangChain v1 `createAgent` 调用、最终消息解析和工具调用摘要提取。
- `src/types.ts`：共享类型、公共状态结构和产物结构。
- `src/artifacts.ts`：最终产物写入和路径展示。
- `src/memory/`：运行状态和长期记忆扩展接口。

禁止：

- 在 `src/agents/` 中写大段提示词。
- 在 prompt 文件中调用工具、写文件或修改状态。
- 在工具文件中直接调用 LLM。
- 在 CLI 中硬编码某个业务 Agent 的执行逻辑。

## 4. 新增 Agent 流程

新增一种能力时，按以下顺序改动：

1. 新建 `src/agents/<flow>/` 目录。
2. 在 `src/agents/<flow>/prompts.ts` 新增对应 prompt builder。
3. 如需当前 flow 专用能力，在 `src/agents/<flow>/tools/` 新增工具封装；只有跨业务可复用的基础能力才放入 `src/tools/`。
4. 在 `src/agents/<flow>/agents.ts` 新增或扩展 ReAct Agent 节点，节点通过 `runReactToolAgent(...)` 调用本 flow 的工具集合。
5. 在 `src/agents/<flow>/pluginData.ts` 定义 route、私有状态类型，并继承 `PluginDataStore<T>` 导出 flow 专属 store。
6. 在 `src/agents/<flow>/flow.ts` 注册 flow definition，包括 route、description、nodes、startNode、edges 和必要的 conditionalEdges。
7. 在 `src/graph/agentRegistry.ts` 聚合导入该 flow definition。
8. 如最终需要生成内容产物，通过 `writeAgentArtifact` 写入 `output/<finalAgent>/`。
9. 更新 README 或本规范中的架构说明。
10. 运行 `pnpm type-check`。

## 5. Agent 节点标准

每个 Agent 节点必须符合统一签名：

```ts
export async function xxxAgent(
  state: AgentState,
  runtime: AgentRuntime,
): Promise<Partial<AgentState>> {
  // ...
}
```

节点执行要求：

- 函数开头定义 `const nodeName = "xxxAgent"`。
- 开始时调用 `runtime.logger.nodeStart(nodeName, summary)`。
- 成功时调用 `runtime.logger.nodeSuccess(nodeName, summary)`。
- 需要调试中间状态时调用 `runtime.logger.debug(...)`，不要直接 `console.log`。
- 失败时调用 `runtime.logger.nodeError(nodeName, error)`，并返回 `errors` 更新。
- 节点返回值只包含本节点修改的状态字段。
- 如果失败后不应继续执行，设置 `finalAnswer`，由 graph 的 `continueOrEnd` 提前结束。
- 业务流程中间态必须通过本 flow 的 `PluginDataStore` 子类读写，不要直接散落 `unknown` 类型断言。
- ReAct 节点应调用 `runReactToolAgent(...)`，不要在节点内手写 `createAgent`、`bindTools`、`ToolNode` 或模型工具循环。
- ReAct 节点只保存工具调用摘要、最终回答和产物记录；不要把完整消息历史、完整网页内容或完整命令日志写入 `pluginData`。

## 6. 提示词标准

提示词按职责维护：

- 路由提示词：`routerPrompts.ts`
- JSON 修复提示词：`jsonRepairPrompts.ts`
- 业务 flow 提示词：`src/agents/<flow>/prompts.ts`

prompt builder 命名规则：

```ts
export function buildXxxPrompt(...): string {
  return `...`;
}
```

要求：

- Agent 文件只能调用 `buildXxxPrompt(...)`，不能内联大段模板字符串。
- 结构化输出必须明确要求 JSON，并配合 zod schema 校验；ReAct flow 的系统提示词必须明确说明可用工具、工具调用顺序约束和最终回答要求。
- prompt builder 只接收必要上下文，不接收完整 `AgentState`，避免和业务状态强耦合。
- prompt 文件不得导入 LLM、工具、logger 或 artifacts。

## 7. 状态设计标准

`AgentState` 顶层只保存跨流程公共数据，例如输入、工作目录、运行参数、路由结果、最终回答、产物、错误、事件，以及后续任务规划或多 Agent 协作需要共享的公共上下文。

业务流程私有中间态必须放入 `pluginData[route]`，并通过本 flow 的 `PluginDataStore` 子类读写。

新增状态字段要求：

- 只有任务规划、多 Agent 协作、全局审计等跨流程公共数据，才允许新增 `AgentState` 顶层字段，并同步更新 `AgentStateAnnotation`。
- 单个业务 flow 的搜索结果、摘要、命令计划、风险结果、执行结果等中间态不得新增为顶层字段。
- `pluginData` key 必须使用该 flow 的 route。
- `PluginDataStore` 子类应不可变更新 `pluginData`，节点返回 `{ pluginData: nextPluginData }`。
- 不要把大文件内容、二进制内容或无界日志放进 `AgentState` 或 `pluginData`。
- 后续需要长期记忆的数据，应设计成可序列化结构。

如果新增公共结构体，必须在 `src/types.ts` 定义 interface/type，并加中文 JSDoc；如果新增业务私有结构体，优先在 `src/agents/<flow>/pluginData.ts` 定义。

## 8. 产物标准

默认不保存过程产物；只有明确需要文件产物的任务才保存最终结果产物。

规则：

- 资料型最终产物写入 `output/researchReactAgent/<runId>-final.md`。
- 行政边界产物写入 `output/boundaryReactAgent/`。
- 命令型任务默认不写反馈产物，成功、取消、拦截或失败原因直接输出到终端。
- 新增最终 Agent 时使用 `writeAgentArtifact(state, runtime, ...)`。
- 写入成功后用 `appendArtifact(state, artifact)` 更新状态。
- 需要文件产物时，`finalAnswer` 只写简短说明和文件路径。
- 不需要文件产物时，`finalAnswer` 直接写简短结果；命令失败时应包含可能原因。
- 终端展示运行状态、最终结果和必要的产物路径。

只有在用户明确要求审计、调试或保存过程时，才新增过程产物策略。

## 9. 工具和安全标准

工具封装按复用边界分层，职责边界必须清晰：

- 面向 ReAct Agent 暴露的工具必须是 LangChain 标准工具，使用 `import { tool } from "langchain"` 创建，并提供稳定的 `name`、`description` 和 zod `schema`。
- 单个业务 flow 强依赖的工具放在 `src/agents/<flow>/tools/`，不要放入公共 `src/tools/`。
- `src/tools/` 只保留跨业务 flow 复用的基础能力，不放业务专属 ReAct 工具集合。
- 网络搜索统一封装，不让 Agent 直接依赖第三方返回结构。
- 本地命令执行必须经过风险检查；确认策略由风险等级决定。
- `blocked` 命令必须由代码规则拦截，不靠 prompt 约束代替代码约束。
- 工具函数不直接修改 `AgentState`，只返回结构化结果。
- 工具可以通过闭包上下文收集最终产物记录，但状态更新必须由 Agent 节点统一返回。
- 大体积数据不得直接返回给模型；例如完整 GeoJSON、大段网页正文和完整命令日志应在工具内截断、缓存或摘要化。
- 涉及文件写入时优先写到受控目录，避免覆盖用户文件。

命令型 Agent 必须遵守：

- 命令执行工具内部必须重新运行 `checkCommandRisk`，不能信任模型、prompt 或先前风险评估工具的输出。
- 命令型 ReAct flow 需要补充用户信息时，必须通过工具读取终端输入；不要把追问作为最终回答直接结束。
- `blocked` 风险命令禁止执行。
- `high` 风险命令必须人工确认后执行。
- `medium` 和 `low` 风险命令通过风险检查后直接执行。
- `--yes` 只跳过确认，不跳过风险检查。
- 任意命令失败后停止后续命令。
- stdout/stderr 捕获必须有长度限制。
- 同一轮 ReAct 运行中允许重复执行只读检查，但禁止重复执行有副作用命令计划，避免模型循环造成重复副作用。
- 同一轮 ReAct 运行中的用户追问次数必须有限制，避免模型无限等待或反复追问。

## 10. 日志标准

所有任务节点执行时必须使用 `Logger`：

- 普通运行：展示节点开始、完成、失败、最终结果和必要的产物路径。
- `--verbose`：展示路由结果、ReAct 工具调用摘要、搜索结果摘要、命令计划摘要、风险结果和执行结果摘要等中间状态。
- 禁止在业务节点中直接使用 `console.log` 输出流程日志。
- 允许 CLI 层输出最终汇总标题和产物清单。

## 11. 测试和验收

每次新增或修改 Agent 后必须执行：

```bash
pnpm type-check
```

验收清单：

- 所有新增 `src/**/*.ts` 文件有文件头注释。
- 所有业务函数有中文 JSDoc。
- Agent 文件没有内联大段提示词。
- 新业务流程已在 `src/agents/<flow>/flow.ts` 注册 flow definition，并在 `src/graph/agentRegistry.ts` 聚合。
- ReAct flow 已通过 `runReactToolAgent(...)` 调用 LangChain v1 `createAgent`，没有使用已弃用的 `createReactAgent`。
- 面向 ReAct 的工具使用 `langchain` 的 `tool(...)` 创建，并声明 zod schema。
- 业务私有状态已通过本 flow 的 `PluginDataStore` 子类读写。
- 新增 `AgentState` 顶层字段时，确认它是跨流程公共数据，并同步 `AgentStateAnnotation`。
- 需要最终产物时只保存最终产物。
- 命令型流程没有绕过风险检查，执行工具内部会重新检查风险并限制重复执行。
- README 或规范文档已更新必要说明。

## 12. 推荐模板

新增 Agent 文件模板：

```ts
/*
 * @Author: wanglinglei
 * @Date: 2026-05-27 19:16:50
 * @Description: 实现 xxx Agent 节点。
 * @FilePath: /agents-cli/src/agents/xxx/agents.ts
 * @LastEditTime: 2026-05-27 19:16:50
 */
import type { AgentRuntime, AgentState } from "../../types.js";
import { runReactToolAgent } from "../../graph/reactToolRunner.js";
import { createXxxTools } from "./tools/xxxTools.js";
import { truncateText } from "../../text.js";
import { buildXxxReactPrompt } from "./prompts.js";
import { xxxPluginData } from "./pluginData.js";

/**
 * xxx Agent。
 *
 * 输入用户任务，调用 LangChain 标准工具完成本 flow 的 ReAct 工具调用流程，并把
 * 工具调用摘要和最终回答写入本 flow 私有状态。
 */
export async function xxxAgent(
  state: AgentState,
  runtime: AgentRuntime,
): Promise<Partial<AgentState>> {
  const nodeName = "xxxAgent";
  runtime.logger.nodeStart(nodeName, truncateText(state.input));

  try {
    const result = await runReactToolAgent({
      nodeName,
      prompt: buildXxxReactPrompt(state.input),
      state,
      runtime,
      tools: createXxxTools({ state, runtime }),
    });

    runtime.logger.nodeSuccess(nodeName, truncateText(result.finalAnswer));
    runtime.logger.debug("ReAct 工具调用摘要", result.toolEvents);

    return {
      pluginData: xxxPluginData.update(state, {
        toolEvents: result.toolEvents,
        finalContent: result.finalAnswer,
      }),
      finalAnswer: result.finalAnswer,
    };
  } catch (error) {
    runtime.logger.nodeError(nodeName, error);
    return {
      errors: [...state.errors, error instanceof Error ? error.message : String(error)],
      finalAnswer: "xxx Agent 执行失败。",
    };
  }
}
```
