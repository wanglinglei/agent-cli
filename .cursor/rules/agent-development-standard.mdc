---
description: agents-cli Agent 扩展开发规范
globs:
  - "src/**/*.ts"
  - "README.md"
alwaysApply: true
---

# Agent 开发规范

本文档定义 `agents-cli` 后续扩展 Agent 时必须遵守的结构、命名、提示词、状态、日志、产物和验证标准。

## 1. 架构原则

- CLI 只有一个自然语言入口：`src/cli.ts`。
- 所有任务先进入 `routerAgent`，由 LangGraph 条件分支选择后续 Agent 流程。
- 新增能力优先扩展 `RouteType`、`routerAgent` 提示词和 `src/graph/index.ts`，不要新增独立 CLI 入口。
- Agent 节点只负责业务流程：准备上下文、调用 prompt builder、调用模型或工具、更新 `AgentState`。
- 提示词、工具、状态类型、产物写入必须分层维护，不能混在 Agent 业务逻辑里。

## 2. 文件规范

所有 `src/**/*.ts` 文件必须带文件头注释：

```ts
/*
 * @Author: wanglinglei
 * @Date: 2026-05-27 19:16:50
 * @Description: 当前文件主要功能说明。
 * @FilePath: /agents-cli/src/path/to/file.ts
 * @LastEditTime: 2026-05-27 19:16:50
 */
```

要求：

- `@Description` 必须用中文说明当前文件职责。
- `src/cli.ts` 保留 shebang 第一行，文件头注释放在 shebang 后面。
- 新文件默认使用 ASCII；中文只用于注释、提示词和面向用户输出。
- 业务函数必须添加中文 JSDoc，说明输入、输出、状态变更和失败策略。

## 3. 目录职责

- `src/agents/`：Agent 节点实现，只写流程逻辑。
- `src/prompts/`：所有 LLM 提示词模板和 prompt builder。
- `src/tools/`：外部工具、本地命令、风险检查、搜索等可复用能力。
- `src/graph/`：LangGraph 状态定义、节点注册、边和条件分支。
- `src/types.ts`：共享类型、状态结构、路由枚举和产物结构。
- `src/artifacts.ts`：最终产物写入和路径展示。
- `src/memory/`：运行状态和长期记忆扩展接口。

禁止：

- 在 `src/agents/` 中写大段提示词。
- 在 prompt 文件中调用工具、写文件或修改状态。
- 在工具文件中直接调用 LLM。
- 在 CLI 中硬编码某个业务 Agent 的执行逻辑。

## 4. 新增 Agent 流程

新增一种能力时，按以下顺序改动：

1. 在 `src/types.ts` 扩展 `RouteType` 和相关状态字段。
2. 在 `src/prompts/routerPrompts.ts` 增加路由说明。
3. 在 `src/prompts/*Prompts.ts` 新增对应 prompt builder。
4. 在 `src/agents/` 新增或扩展 Agent 节点。
5. 如需外部能力，在 `src/tools/` 新增工具封装。
6. 在 `src/graph/index.ts` 注册节点、边和条件分支。
7. 如最终需要生成内容产物，通过 `writeAgentArtifact` 写入 `output/<finalAgent>/`。
8. 更新 README 或本规范中的架构说明。
9. 运行 `pnpm type-check`。

## 5. Agent 节点标准

每个 Agent 节点必须符合统一签名：

```ts
export async function xxxAgent(
  state: AgentState,
  runtime: AgentRuntime,
): Promise<Partial<AgentState>> {
  // ...
}
```

节点执行要求：

- 函数开头定义 `const nodeName = "xxxAgent"`。
- 开始时调用 `runtime.logger.nodeStart(nodeName, summary)`。
- 成功时调用 `runtime.logger.nodeSuccess(nodeName, summary)`。
- 需要调试中间状态时调用 `runtime.logger.debug(...)`，不要直接 `console.log`。
- 失败时调用 `runtime.logger.nodeError(nodeName, error)`，并返回 `errors` 更新。
- 节点返回值只包含本节点修改的状态字段。
- 如果失败后不应继续执行，设置 `finalAnswer`，由 graph 的 `continueOrEnd` 提前结束。

## 6. 提示词标准

提示词必须集中维护在 `src/prompts/`：

- 路由提示词：`routerPrompts.ts`
- 资料型提示词：`researchPrompts.ts`
- 命令型提示词：`commandPrompts.ts`
- JSON 修复提示词：`jsonRepairPrompts.ts`

prompt builder 命名规则：

```ts
export function buildXxxPrompt(...): string {
  return `...`;
}
```

要求：

- Agent 文件只能调用 `buildXxxPrompt(...)`，不能内联大段模板字符串。
- 结构化输出必须明确要求 JSON，并配合 zod schema 校验。
- prompt builder 只接收必要上下文，不接收完整 `AgentState`，避免和业务状态强耦合。
- prompt 文件不得导入 LLM、工具、logger 或 artifacts。

## 7. 状态设计标准

所有跨节点数据必须定义在 `AgentState` 中，并同步更新 `AgentStateAnnotation`。

新增状态字段要求：

- 字段名语义清晰，避免缩写。
- 可选中间结果使用 `xxx?: Type`。
- 数组字段必须在 `createInitialState` 中初始化。
- 不要把大文件内容、二进制内容或无界日志放进状态。
- 后续需要长期记忆的数据，应设计成可序列化结构。

如果新增结构体，必须在 `src/types.ts` 定义 interface/type，并加中文 JSDoc。

## 8. 产物标准

默认只保存最终结果产物，不保存过程产物。

规则：

- 资料型最终产物写入 `output/formatAgent/<runId>-final.md`。
- 命令型最终反馈写入 `output/feedbackAgent/<runId>-final.md`。
- 新增最终 Agent 时使用 `writeAgentArtifact(state, runtime, ...)`。
- 写入成功后用 `appendArtifact(state, artifact)` 更新状态。
- `finalAnswer` 只写简短说明和文件路径，不直接放大段正文。
- 终端只展示运行状态和最终产物路径。

只有在用户明确要求审计、调试或保存过程时，才新增过程产物策略。

## 9. 工具和安全标准

工具封装放在 `src/tools/`，职责边界必须清晰：

- 网络搜索统一封装，不让 Agent 直接依赖第三方返回结构。
- 本地命令执行必须经过风险检查和用户确认。
- 高危命令默认拦截，不靠 prompt 约束代替代码约束。
- 工具函数不直接修改 `AgentState`，只返回结构化结果。
- 涉及文件写入时优先写到受控目录，避免覆盖用户文件。

命令型 Agent 必须遵守：

- 默认人工确认后执行。
- `--yes` 只跳过确认，不跳过风险检查。
- 任意命令失败后停止后续命令。
- stdout/stderr 捕获必须有长度限制。

## 10. 日志标准

所有任务节点执行时必须使用 `Logger`：

- 普通运行：展示节点开始、完成、失败和最终产物路径。
- `--verbose`：展示路由结果、搜索结果、命令计划、风险结果、执行结果等中间状态。
- 禁止在业务节点中直接使用 `console.log` 输出流程日志。
- 允许 CLI 层输出最终汇总标题和产物清单。

## 11. 测试和验收

每次新增或修改 Agent 后必须执行：

```bash
pnpm type-check
```

验收清单：

- 所有新增 `src/**/*.ts` 文件有文件头注释。
- 所有业务函数有中文 JSDoc。
- Agent 文件没有内联大段提示词。
- 新状态已同步 `AgentState` 和 `AgentStateAnnotation`。
- 需要最终产物时只保存最终产物。
- 命令型流程没有绕过风险检查。
- README 或规范文档已更新必要说明。

## 12. 推荐模板

新增 Agent 文件模板：

```ts
/*
 * @Author: wanglinglei
 * @Date: 2026-05-27 19:16:50
 * @Description: 实现 xxx Agent 节点。
 * @FilePath: /agents-cli/src/agents/xxxAgents.ts
 * @LastEditTime: 2026-05-27 19:16:50
 */
import type { AgentRuntime, AgentState } from "../types.js";

/**
 * xxx Agent。
 *
 * 说明该节点读取哪些状态、生成哪些状态，以及失败后是否终止流程。
 */
export async function xxxAgent(
  state: AgentState,
  runtime: AgentRuntime,
): Promise<Partial<AgentState>> {
  const nodeName = "xxxAgent";
  runtime.logger.nodeStart(nodeName, state.input);

  try {
    runtime.logger.nodeSuccess(nodeName, "完成 xxx");
    return {};
  } catch (error) {
    runtime.logger.nodeError(nodeName, error);
    return {
      errors: [...state.errors, error instanceof Error ? error.message : String(error)],
      finalAnswer: "xxx Agent 执行失败。",
    };
  }
}
```
